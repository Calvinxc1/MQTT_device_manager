summary: Creates a new device
description: |
    Takes as input a device name and location, and creates a new device for it.
requestBody:
    $ref: '#/components/requestBodies/DevicePostReq'
responses:
    200:
        $ref: '#/components/responses/200-DevicePostResp'
    400:
        $ref: '#/components/responses/400-UserError'
    500:
        $ref: '#/components/responses/500-ServerError'
tags:
- Device

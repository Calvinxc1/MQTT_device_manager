content:
    application/json:
        schema:
            type: object
            properties:
                name:
                    $ref: '#/components/schemas/deviceName'
                location:
                    $ref: '#/components/schemas/deviceLocation'
            required:
            - name
        example:
            name: Example Device
            location: Garden
